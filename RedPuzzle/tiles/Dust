return function()
	return {
		color = colors.red;
		char = "*";
		powered = function(self, _power)
			for k, v in pairs(_power)
				if v == self.color then
					return sides.set
				end
			end
			return {}
		end;
	}
end